# Unit-02 NFR要件プラン

## 前提
- Unit-02（日誌・感情記録コア）は Unit-01（認証・テナント基盤）のテックスタック・インフラ・NFRを全継承する
- 本プランでは Unit-02 固有の NFR 決定事項のみを確認する

## 実行ステップ

- [x] Step 1: 機能設計の読み込み（business-logic-model / domain-entities / frontend-components / business-rules）
- [x] Step 2: Unit-01 継承 NFR の特定
- [x] Step 3: Unit-02 固有 NFR 論点の抽出
- [x] Step 4: 質問への回答を受領（Q1-5=A / Q6=B / Q7=A）
- [x] Step 5: 回答の曖昧さを検証（曖昧さなし）
- [x] Step 6: nfr-requirements.md を最終化
- [x] Step 7: tech-stack-decisions.md を作成（Unit-01継承を明示）
- [x] Step 8: 完了メッセージを提示し承認待ち

---

## Unit-01 から継承する NFR（再決定不要）

| カテゴリ | 継承内容 |
|---|---|
| パフォーマンス | API P95 500ms・ページロード P95 3秒 |
| 可用性 | 99.5%（単一AZ・RDS 日次バックアップ7日保持） |
| セキュリティ | Security Baseline（SECURITY-01〜15）全適用 |
| スケーラビリティ | RDS Proxy・Vercel 自動スケール |
| ログ/監視 | pino+redact・CloudWatch Logs 90日保持 |
| テスト | ビジネスロジック 80% カバレッジ・Vitest |
| テックスタック | Next.js 14 Pages Router・Auth.js v4・PostgreSQL 16・Drizzle ORM・Zod |

---

## Unit-02 固有 NFR 論点

### 論点 1: タグ一覧の表示件数上限

機能設計で「テナント内全タグをボタン表示」が確定。タグが増えた場合の UI・パフォーマンス戦略を確認したい。

**Q1**: エントリ作成フォームでのタグ表示上限は？
- A. 20件（超過時はテキスト入力でフィルタ）
- B. 50件（超過時はテキスト入力でフィルタ）
- C. 上限なし（全件常時表示）
- D. その他（自由記述）

[Answer]: 

---

### 論点 2: タイムラインのキャッシュ戦略

共有タイムライン（テナント内公開エントリ）は読み取り頻度が高い。ISR/SWRキャッシュで高速化するか、常に最新を取りにいくか。

**Q2**: 共有タイムライン（`GET /api/journal/entries`）のキャッシュ方針は？
- A. stale-while-revalidate 30秒（30秒以内はキャッシュ即返・バックグラウンド再検証）
- B. キャッシュなし（常に DB 最新取得）
- C. 短期キャッシュ 10秒のみ
- D. その他（自由記述）

[Answer]: 

---

### 論点 3: マイ記録のキャッシュ

自分の非公開エントリを含むマイ記録はプライバシー観点でキャッシュを避けたい。

**Q3**: マイ記録（`GET /api/journal/entries/mine`）のキャッシュ方針は？
- A. キャッシュなし（常に最新・非公開エントリを含むため）
- B. プライベートキャッシュ 10秒（Cache-Control: private, max-age=10）
- C. その他（自由記述）

[Answer]: 

---

### 論点 4: システムデフォルトタグのシード

機能設計でテナント作成時にデフォルトタグをシードする方針。シード対象のタグセットを確認したい。

**Q4**: テナント作成時にシードするシステムデフォルトタグは？
- A. 感情5件（うれしい・つかれた・やってみた・行き詰まり・相談したい）+ カテゴリ3件（授業準備・保護者対応・行事準備）= 8件
- B. 感情のみ5件
- C. シードなし（全タグを教員が自由作成）
- D. その他（具体的に記述）

[Answer]: 

---

### 論点 5: タグ削除時の挙動

school_admin がタグ削除した場合、既存エントリとの紐づきをどうするか。

**Q5**: タグ削除時の既存エントリへの影響は？
- A. CASCADE: journal_entry_tags から紐づきのみ削除、エントリ本体は残る
- B. 削除前にエントリ数を表示して確認（該当エントリがある場合は警告）
- C. 使用中タグは削除不可
- D. その他

[Answer]: 

---

### 論点 6: 文字数制限の強制箇所

機能設計で 200文字制限が確定。バリデーションを多層で行うか。

**Q6**: 200文字制限のバリデーション層は？
- A. クライアント（Zod）+ API 層（Zod）+ DB 制約（CHECK）の三層
- B. クライアント + API 層のみ
- C. API 層のみ
- D. その他

[Answer]: 

---

### 論点 7: タイムラインのページサイズ

機能設計で「20件/ページ」が確定しているが、モバイル閲覧時のUXで調整するか確認。

**Q7**: タイムラインのページサイズは確定通り 20件/ページ でよいか？
- A. はい、20件で確定
- B. モバイルは10件、デスクトップは20件
- C. 無限スクロール（ページネーションなし）
- D. その他

[Answer]: 

---

## 回答後の成果物

1. `aidlc-docs/construction/unit-02/nfr-requirements/nfr-requirements.md`（既存を更新）
2. `aidlc-docs/construction/unit-02/nfr-requirements/tech-stack-decisions.md`（新規・Unit-01継承を明記）
