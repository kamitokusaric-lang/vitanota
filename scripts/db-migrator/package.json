{
  "name": "vitanota-db-migrator",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda function for running Drizzle migrations against RDS Proxy via IAM auth",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "package": "pnpm build && cd dist && zip -r ../function.zip . && cd .. && zip -ru function.zip migrations node_modules"
  },
  "dependencies": {
    "@aws-sdk/rds-signer": "^3.574.0",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "@types/pg": "^8.11.6",
    "typescript": "^5.4.5"
  }
}
